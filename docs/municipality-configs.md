# Municipality Configs — tools/municipality-configs/

Per-municipality YAML scraper configurations.

## Convention
- All municipality-specific URLs and scraping rules live here as YAML files
- Never hardcode municipality URLs in service code
- Scraper service reads these configs at runtime
